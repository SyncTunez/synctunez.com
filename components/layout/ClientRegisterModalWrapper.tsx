"use client";
import RegisterModalWrapper from "@/components/RegisterModalWrapper";

export default function ClientRegisterModalWrapper(props: React.ComponentProps<typeof RegisterModalWrapper>) {
  return <RegisterModalWrapper {...props} />;
} 